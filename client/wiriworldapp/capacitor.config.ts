import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'wiriworld.com',
  appName: 'wiriworldapp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
